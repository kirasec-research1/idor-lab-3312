merge_protections:
  - name: code review required
    if:
      - base = main
    success_conditions:
      - "#approved-reviews-by >= 1"

queue_rules:
  - name: default
    queue_conditions:
      - "-draft"
    merge_conditions: []
